{
  "name": "lift-cloudflare",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:apply:local": "wrangler d1 migrations apply lift-auth --local",
    "db:apply:remote": "wrangler d1 migrations apply lift-auth --remote"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "~5.9.0",
    "wrangler": "^4.0.0"
  }
}
